- name: Create empty mongo-shard-client-vault-agent.pid file
  file:
    path: "{{ vault_dir }}/agents/mongo/shard-client/mongo-shard-client-vault-agent.pid"
    state: touch
    owner: "{{ mongo_vault_agent_user }}"
    group: "{{ mongo_vault_agent_group }}"
    mode: "0640"
  become: true

- name: Create vault policies
  block:
    - name: "Render Vault policy for mongo-shard-client-approle-reader"
      template:
        src: "{{ playbook_dir }}/../vault/policies/mongo-shard-client-approle-reader.hcl.j2"
        dest: "/tmp/mongo-shard-client-approle-reader-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.hcl"

    - name: "Read rendered mongo-shard-client-approle-reader policy file"
      slurp:
        src: "/tmp/mongo-shard-client-approle-reader-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.hcl"
      register: slurped_mongo_shard_client_approle_reader_policy

    - name: "Upload mongo-shard-client-approle-reader policy to vault"
      delegate_to: localhost
      community.hashi_vault.vault_write:
        url: "{{ vault_url }}"
        token: "{{ vault_token }}"
        path: "sys/policies/acl/mongo-shard-client-approle-reader-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}"
        ca_cert: "{{ local_ca_path }}"
        data:
          policy: "{{ slurped_mongo_shard_client_approle_reader_policy.content | b64decode }}"

    - name: "Render Vault policy for mongo-shard-client"
      template:
        src: "{{ playbook_dir }}/../vault/policies/mongo-shard-client.hcl.j2"
        dest: "/tmp/mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.hcl"

    - name: "Read rendered mongo-shard-client policy file"
      slurp:
        src: "/tmp/mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.hcl"
      register: slurped_mongo_shard_client_policy

    - name: "Upload mongo-shard-client policy to vault"
      delegate_to: localhost
      community.hashi_vault.vault_write:
        url: "{{ vault_url }}"
        token: "{{ vault_token }}"
        path: "sys/policies/acl/mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}"
        ca_cert: "{{ local_ca_path }}"
        data:
          policy: "{{ slurped_mongo_shard_client_policy.content | b64decode }}"

  always:
    - name: "Clean up temporary policy file"
      file:
        path: "/tmp/mongo-shard-client-approle-reader-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.hcl"
        state: absent

    - name: "Clean up temporary policy file"
      file:
        path: "/tmp/mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.hcl"
        state: absent

- name: "Create AppRole for node"
  delegate_to: localhost
  community.hashi_vault.vault_write:
    url: "{{ vault_url }}"
    token: "{{ vault_token }}"
    path: "auth/approle/role/mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}"
    ca_cert: "{{ local_ca_path }}"
    data:
      token_policies: "mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}"
      token_ttl: "1h"
      token_max_ttl: "4h"

- name: Copy Mongo-Shard-Client Vault Agent
  template:
    src: "{{ playbook_dir }}/../vault/agents/mongo-shard-client-agent-config.hcl.j2"
    dest: "{{ vault_dir }}/agents/mongo/shard-client/mongo-shard-client-agent-config.hcl"
    owner: "{{ mongo_vault_agent_user }}"
    group: "{{ mongo_vault_agent_group }}"
    mode: "0400"

- name: Deploy rendered Vault Agent mongo-shard-client.pem.ctmpl template
  template:
    src: "{{ playbook_dir }}/../vault/templates/mongo-shard-client.pem.ctmpl.j2"
    dest: "{{ vault_dir }}/templates/mongo/shard-client/mongo-shard-client-10.{{ mongo_shard_subnet }}.0.{{ ansible_host.split('.')[-1] }}.pem.ctmpl"
    owner: "{{ mongo_vault_agent_user }}"
    group: "{{ mongo_vault_agent_group }}"
    mode: "0400"

- name: Install mongo-shard-client-approle-vault-server-request.conf
  template:
    src: mongo-shard-client-approle-vault-server-request.conf.j2
    dest: "{{ mongo_base_dir }}/conf/mongo-shard-client-approle-vault-server-request.conf"
    owner: "{{ mongo_vault_agent_user }}"
    group: "{{ mongo_vault_agent_group }}"
    mode: "0440"

- name: Install fetch-vault-mongo-shard-client-approle-ids script
  template:
    src: fetch-vault-mongo-shard-client-approle-ids.sh.j2
    dest: "{{ vault_dir }}/scripts/mongo/shard-client/fetch-vault-mongo-shard-client-approle-ids.sh"
    owner: "{{ mongo_vault_agent_user }}"
    group: "{{ mongo_vault_agent_group }}"
    mode: "0550"

- name: Create systemd unit to run the fetcher on boot
  copy:
    dest: /etc/systemd/system/fetch-vault-mongo-shard-client-approle-ids.service
    content: |
      [Unit]
      Description=Vault Mongo-Shard-Client approle ids fetcher
      After=network-online.target
      Wants=network-online.target

      [Service]
      User=root
      Group={{ vault_agent_bin_group }}
      Type=oneshot
      ExecStartPre=/bin/mkdir -p {{ vault_dir }}/secrets/mongo/shard-client
      ExecStart=/bin/bash {{ vault_dir }}/scripts/mongo/shard-client/fetch-vault-mongo-shard-client-approle-ids.sh
      StandardOutput=journal
      StandardError=journal
      RemainAfterExit=yes

      [Install]
      WantedBy=multi-user.target
  notify: Reload systemd

- name: Create systemd service file for mongo-shard-client vault agent
  copy:
    dest: /etc/systemd/system/mongo-shard-client-vault-agent.service
    content: |
      [Unit]
      Description=Vault Agent
      After=network.target fetch-vault-mongo-shard-client-approle-ids.service
      Requires=fetch-vault-mongo-shard-client-approle-ids.service

      [Service]
      User={{ mongo_vault_agent_user }}
      Group={{ mongo_vault_agent_group }}
      ExecStart={{ vault_dir }}/bin/vault agent -config={{ vault_dir }}/agents/mongo/shard-client/mongo-shard-client-agent-config.hcl
      Restart=on-failure
      PIDFile={{ vault_dir }}/agents/mongo/shard-client/mongo-shard-client-vault-agent.pid

      [Install]
      WantedBy=multi-user.target
  notify: Reload systemd

- name: Force Reload systemd
  systemd:
    daemon_reload: yes

- name: Enable & start fetcher
  systemd:
    name: fetch-vault-mongo-shard-client-approle-ids.service
    state: started
    enabled: yes
  register: fetcher_status

- name: Start and enable Mongo-Shard-Client Vault Agent service
  systemd:
    name: mongo-shard-client-vault-agent
    state: started
    enabled: yes
