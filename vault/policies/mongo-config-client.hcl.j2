path "auth/approle/login" {
  capabilities = ["create"]
}

path "pki/issue/mongo-config-client-10.{{ mongo_config_subnet }}.0.{{ ansible_host.split('.')[-1] }}" {
  capabilities = ["create", "update", "read", "list"]
}
